# Contributing

Keep changes focused, preserve the main/renderer/protocol boundaries in `AGENTS.md`, and use
Conventional Commit messages. Before opening a pull request, run:

```bash
npm run format:check
npm run lint
npm test
npm run build
```

Do not commit credentials, private logs, local settings, user pet packs, copyrighted character
assets, or generated build output.
